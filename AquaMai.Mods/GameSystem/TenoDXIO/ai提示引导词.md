请帮我重构 TenoDX 游戏区域的判定逻辑与配置系统。目前游戏区域有 A, B, C, D, E 共 5 种大区类型，对应物理上不同的区块。大区字母后的数字代表该区内的具体编号。

请基于以下要求修改相关 C# 代码：

### 1. 通道映射与区块信息获取
这是物理通道 (0-33) 到逻辑区域的映射表：
* Touch0 (0x08): 0-A5, 1-E5, 2-D5, 3-B4, 4-A4, 5-E4, 6-D4, 7-B3, 8-A3, 9-C1, 10-E3, 11-D3, 12-B2, 13-A2, 14-E2, 15-D2, 16-B1
* Touch1 (0x09): 17-A1, 18-E1, 19-D1, 20-B8, 21-A8, 22-E8, 23-D8, 24-B7, 25-A7, 26-C2, 27-E7, 28-D7, 29-B6, 30-A6, 31-E6, 32-D6, 33-B5

**任务：**
* 编写一个独立函数，输入物理通道号(0-33)，根据上述映射表返回其所属的大区(char)和编号(int)。
* 将所有通道按算法逻辑分为 3 个组：组0（处理 A 区），组1（处理 C 区），组2（处理 B、D、E 区）。判定器在获取区块信息后，需调用对应的组逻辑。

### 2. 全局与变量修改
* 将原代码中的局部变量 `deriv` 统一重命名为 `diff_deriv`。

### 3. TenoDXIO.cs 配置文件系统重构
请对配置类进行以下修改：
1. **删除**原有的通道映射配置功能（改用上述硬编码的函数实现）。
2. **日志系统**：修改配置，允许用户配置“需要输出日志的区域”。
3. **波特率**：移除波特率的配置项，在串口初始化中直接硬编码使用 115200。
4. **单通道覆盖配置系统重构**：
   * 将原本的配置映射改为纯字符串（String）类型的输入框系统。
   * 用户输入格式为 `"通道名:数值"` 并以逗号分割（例如 `A1:600, C2:25`）。未填写的通道使用该区的默认值。
   * **跨区混合填写支持**：在任一覆盖输入框内，允许跨区填写（例如在触发线输入框填写 `B1:10, D3:12, E4:16`）。你需要设计相应的解析逻辑（如返回 Dictionary 或专门的配置类）供判定器读取。

### 4. 各大区判定参数与逻辑实现
判定逻辑与配置输入框需按以下规则对应（默认值如果配置文件未填则使用下值）：

**【A 区】**（保持原有复杂逻辑结构，默认值如下）
* 基础触发灵敏度: 650
* 长按防断触: 450
* 连击急速抬手: 1200
* 悬空防触发拦截(diff): 1500
* 悬空防触发拦截(diff_deriv): 15
* 急速抽离灵敏度: -250

**【C 区】**
* **配置输入框**：开放 4 个独立的字符串输入框（对应 4 个参数覆盖）：
  1. `diff` 触发线 (默认: 25)
  2. `diff_deriv` 突变触发线 (默认: 25)
  3. `diff_deriv` 突变触发抑制线 (默认: -20)
  4. `diff` 灵敏度松开线 (默认: 15)
* **判定逻辑** (伪代码)：
  ```python
  if diff > 触发线 or diff_deriv > 突变触发线:
      if diff_deriv < 突变触发抑制线:
          on = False
      else:
          on = True
  elif diff < 灵敏度松开线:
      on = False


- BDE区共同使用以下逻辑 if diff > 15: on = True; if diff_deriv < -16: on = False
        第一个diff注释触发线，第一个diff_deriv注释突变触发抑制